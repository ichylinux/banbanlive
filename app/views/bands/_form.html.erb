<%= form_for @band do |f| %>
  <table>
    <tr>
      <th><%= f.label :name %></th>
      <td><%= f.text_field :name %></td>
    </tr>
  </table>
  
  <%= render 'band_members_fields' %>
  <%= f.submit %>
<% end %>
